import * as React from 'react';
import Rating from '@mui/material/Rating';

export default function RatingSIze() {
  return (
      <Rating style={{marginTop:4}} name="size-medium" defaultValue={2} />
    
  );
}
